
  # Review provided prompt

  This is a code bundle for Review provided prompt. The original project is available at https://www.figma.com/design/xtShNnIV1mThq1G0KTxWzI/Review-provided-prompt.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  